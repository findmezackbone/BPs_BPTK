clear;clc;
%P14.3
%容易知道P14.3中的反向递推公式推出的每项均为0.1
%下考虑其数值计算
u = zeros(1,100);u(100) = 0.1;
v = zeros(1,100);v(100) = 0.1;
w = 0.00001; %第99项扰动
u(99) = 0.1 - w;
v(99) = 0.1 + w;

for i = 98:-1:1
    x = (3*u(i+1) - u(i+2))/2;y = (3*v(i+1) - v(i+2))/2;
    u(i) = x; v(i) = y;
end

s = 0.1*ones(1,100);

plot(1:100,s,'r-d');hold on; plot(1:100,u,'b-d');hold on;plot(1:100,v,'g-d')
bias1 = abs(u(1)-0.1)/0.1;
bias2 = abs(v(1)-0.1)/0.1;